#edit_page.col-md-8.col-md-offset-2
	.row
		.panel.panel-default
			.panel-heading
				%h1 Edit your post
			.panel-body
				.current_image
					%strong.center This is what your picture currently looks like:
					= image_tag @pic.image.url(:medium)
				= render 'form'